#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <math.h>

#define lli long long int
//for loops
#define lfor(i, n, m, indent) for(int i=n; i<m; i+=indent)  //increasing loop
#define rfor(i, n, m, indent) for(int i=n; i>=m; i-=indent) //decreasing loop

int main(){

    return 0;
}